---
workflow: software-delivery-pipeline
runId: <YYYYMMDD-slug>
runPath: workflow/runs/<YYYY-MM-DD>-<slug>
executionMode: full
stage: plan
status: draft
source: user-request
allowsCodeEdit: false
nextAction: confirm_plan
---
# 架构设计与技术选型

## Template Usage
- mode: audited only
- fast 使用 `00-fast-patch-summary.md`
- guarded 使用 `10-guarded-*`

## 文档元信息
- 项目根目录：
- 生成时间：
- 当前分支 / commit：
- 执行 agent：
- 来源文档：
- 文档状态：

## 文档状态
- 待用户确认

## 是否需要独立架构设计
- 是 | 否

## 触发原因
- [ ] 跨模块 / 跨服务
- [ ] 新增或修改持久化结构
- [ ] 新增外部依赖或技术组件
- [ ] 修改 API / 数据契约
- [ ] 修改异步 / MQ / 调度流程
- [ ] 涉及权限 / 安全 / 数据一致性
- [ ] Review handoff 标记为架构级问题
- [ ] 用户明确要求设计 / 架构 / 选型 / 方案
- [ ] 其他：

## 背景与约束
-

## 当前系统现状
-

## 方案选项

### 方案 A
- 说明：
- 优点：
- 缺点：
- 风险：

### 方案 B
- 说明：
- 优点：
- 缺点：
- 风险：

## 选定方案
-

## 选型结论
- 沿用现有技术：
- 新增技术 / 依赖：
- 不采用的方案：

## 架构边界
- 模块边界：
- 服务边界：
- 数据边界：
- 接口边界：

## Provider 侧逻辑设计
- 责任服务 / 模块 / 接口：
- 输入上下文：
- 数据来源：
- 规则 / 策略处理流程：
- 返回的权限决策 / 可操作行为 / DTO 字段：
- 失败行为：fail-closed | 降级 | 返回错误码 | 其他

## Consumer / 调用方逻辑设计
- 调用入口：
- 调用顺序：先查权限范围 | 先查资源再评估 | 其他
- 查询过滤位置：SQL 前置过滤 | 业务层过滤 | 前后结合
- 分页 / 排序 / 总数影响：
- 失败行为：拒绝操作 | 隐藏数据 | 降级展示 | 返回错误码 | 其他

## 接口拆分与取舍
- 候选接口形态：单接口 | scope 查询 + permission 评估 | 其他
- 选择理由：
- 不选方案及原因：
- 性能 / N+1 / 批量能力影响：
- 服务边界 / 复用 / 兼容性影响：

## 权限 / 规则模型设计（如适用）
- 规则表达：强类型枚举 + 策略类 | 规则表 / 配置 | 混合 | 其他
- 策略注册与选择方式：
- 决策上下文：
- 决策结果模型：
- 扩展方式：
- 审计 / 可观测性：

## 数据查询与分页边界
- 可前置过滤的数据：
- 必须由 Provider 评估的资源事实：
- ownership / patientId / 自然人 / 租户 / 时间窗口字段：
- 历史数据兼容：
- 跨服务查询风险：

## 数据与接口契约
-

## 验收项映射
| 验收项 / 风险 | Provider 逻辑 | Consumer 逻辑 | DTO / 字段 | 验证方式 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 兼容性策略
-

## 风险与取舍
-

## 对实施计划的约束
-


## 澄清与收敛记录
- AI 发现的矛盾 / 缺口 / 风险 / 与代码现状冲突：
- 提出的选项或替代方案：
- 用户反馈：
- 本轮更新结果：
- 未解决阻塞项：无 | 列表

## 证据引用
| 结论 | 证据位置 | 类型 | 置信度 | 备注 |
| --- | --- | --- | --- | --- |
|  | `path/to/file:line` | 事实 / 推断 / 待确认 | 高 / 中 / 低 |  |

## 决策记录
| 决策项 | 选择 | 不选方案 | 原因 | 确认记录 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 范围锁定
- 允许修改 / 关注的目录或文件：
- 禁止修改 / 不关注的目录或文件：
- 允许改变的行为：
- 不允许改变的行为：
- 超出范围时的处理：暂停并请求用户确认。

## 影响分析
- 影响模块：
- 影响 API / 接口：
- 影响数据表 / 实体 / DTO：
- 影响配置 / 环境：
- 影响异步任务 / MQ / 调度：
- 影响测试：
- 兼容性影响：
- 回滚影响：

## 变更记录
| 轮次 | 用户反馈 | 文档更新 | 当前状态 |
| --- | --- | --- | --- |
|  |  |  |  |

## 用户确认记录
- 待确认；确认前不得进入实施计划或代码修改阶段。
